# الدليل الهندسي الشامل لواجهة المستخدم — Rabha ERP Frontend Guide

> **دليل توثيقي شامل وتفصيلي لمعمارية الفرونت إند، شرح الملفات، منطق العمليات الحسابية، وتدفق البيانات — كُتب ليكون مرجعاً تقنياً وتنفيذياً كاملاً يمكنك الرجوع إليه وشرح أي تفصيلة في المشروع من خلاله.**

---

## 📑 فهرس المحتويات
1. [المقدمة وفلسفة التصميم (Overview & Architecture Design)](#1-المقدمة-وفلسفة-التصميم)
2. [حزمة التقنيات المستخدمة ولماذا تم اختيارها (Tech Stack & Rationale)](#2-حزمة-التقنيات-المستخدمة)
3. [المبادئ الهندسية الأساسية لتصميم النظام (Core Engineering Principles)](#3-المبادئ-الهندسية-الأساسية)
4. [شرح ملفات التهيئة والبنية التحتية (Core & Configuration Files)](#4-ملفات-التهيئة-والبنية-التحتية)
5. [طبقة الاتصال بالخادم (API Layer)](#5-طبقة-الاتصال-بالخادم-api-layer)
6. [طبقة الأنواع والبيانات (TypeScript Type System)](#6-طبقة-الأنواع-والبيانات)
7. [الأدوات المساعدة والمكونات المشتركة (Utils & Common Components)](#7-الأدوات-المساعدة-والمكونات-المشتركة)
8. [محرك إدارة الحالة وقواعد العمل (HrContext — Business Engine)](#8-محرك-إدارة-الحالة-وقواعد-العمل-hrcontext)
9. [هيكل التطبيق والتنقل العام (Shell, Layout & Navigation)](#9-هيكل-التطبيق-والتنقل-العام)
10. [النوافذ المنبثقة التفاعلية (Modals Layer)](#10-النوافذ-المنبثقة-التفاعلية-modals)
11. [شاشات الموارد البشرية (HR Pages)](#11-شاشات-الموارد-البشرية-hr-pages)
12. [شاشات الرواتب ومسيرات الصرف (Payroll Pages)](#12-شاشات-الرواتب-ومسيرات-الصرف)
13. [شاشات سلاسل الإمداد والمخزون والمشتريات (Supply Chain Pages)](#13-شاشات-سلاسل-الإمداد-والمخزون)
14. [سيناريوهات دورة العمل خطوة بخطوة (End-to-End Real-World Workflows)](#14-سيناريوهات-دورة-العمل-خطوة-بخطوة)
15. [دليل الإجابة على الأسئلة التقنية (Presentation & Interview FAQ)](#15-دليل-الإجابة-على-الأسئلة-التقنية)

---

## 1. المقدمة وفلسفة التصميم

نظام **Rabha ERP** هو نظام إدارة وتخطيط موارد مخصص للمطاعم وسلاسل الإمداد. في قطاع المطاعم، تعتبر السرعة والدقة وسهولة الاستخدام عوامل حاسمة:
- لا يمكن الاعتماد على تحميل صفحات تقليدي بطيء، لذلك تم بناء الفرونت إند بنظام **Single Page Application (SPA)** كامل.
- الواجهة مصممة خصيصاً للمستخدم العربي بأسلوب **RTL Native** (من اليمين لليسار) بخط واضح وألوان دلالية مريحة للعين.
- تم ربط الشاشات بحيث يستطيع المستخدم التنقل بين (شؤون الموظفين، الرواتب، المخزن، المشتريات، الموردين) دون أي وميض أو إعادة تحميل للصفحة.

---

## 2. حزمة التقنيات المستخدمة

| التقنية | الإصدار | سبب الاختيار |
| :--- | :--- | :--- |
| **React** | `^18.3.1` | أحدث معايير React مع Virtual DOM سريع وإدارة متطورة للحالة ودعم لـ Concurrent Rendering. |
| **TypeScript** | `^5.2.2` | يمنع الأخطاء الشائعة (Type Safety)، ويوفر توثيقاً داخلياً لكل كائن، ويضمن عدم تمرير قيم خاطئة للحسابات المالية. |
| **Vite** | `^5.2.11` | أداة بناء فائقة السرعة مع Hot Module Replacement، توفر تجربة تطوير وتشغيل بدون أي تأخير. |
| **Ant Design (AntD)** | `^5.17.0` | أقوى مكتبة مكونات مؤسسية (Enterprise UI)، تدعم الـ RTL واللغة العربية أصلياً مع جداول وفلاتر ونوافذ احترافية جاهزة. |
| **TailwindCSS** | `^3.4.3` | لتنسيق التفاصيل الصغيرة، وتوزيع المساحات (Flexbox & Grid)، وتأثيرات الحركة السريعة (Hover & Transitions). |
| **Lucide React & Ant Icons** | أحدث إصدار | توفير أيقونات واضحة ومعبرة لكل عملية في النظام لتسهيل التعرف البصري السريع لموظفي المطعم. |

---

## 3. المبادئ الهندسية الأساسية

### أ) مبدأ القرش لتفادي أخطاء الحسابات (The Penny / Piaster Rule)
- في لغات البرمجة بما فيها JavaScript، استخدام الكسور العشرية المباشرة (Floats) يسبب أخطاء دقة شهيرة، مثل: `0.1 + 0.2 = 0.30000000000000004`.
- **الحل المطبق في النظام**: يتم التعامل مع أي مبلغ مالي وتخزينه كـ **عدد صحيح بالقرش (Integer Piasters)**:
  - 1000 جنيه مصري = `100000` قرش.
  - جميع عمليات الجمع، الطرح، وحسابات الرواتب تتم على القروش بالكامل.
  - عند العرض للمستخدم فقط على الشاشة، يتم القسمة على `100` وتنسيق المبلغ بالجنيه عبر `formatMoney()`.

### ب) المزامنة المزدوجة للبيانات (Dual State Persistence)
- لكي لا يفقد المستخدم أي حركة حتى لو انقطع الاتصال أو أُغلقت الصفحة:
  1. **الطبقة الأولى (Local Speed)**: تُحفظ كل حركة فوراً في `localStorage` بالمتصفح.
  2. **الطبقة الثانية (Cloud Sync)**: يتم إرسال طلب تزامني للخلفية عبر الـ API (`/api/v1/hr/sync-state`) لحفظ نسخة مطابقة في قاعدة البيانات.

### ج) التناسب الزمني للرواتب (Prorated Base Salary Calculation)
- الموظف الذي ينضم للعمل في منتصف الشهر لا يحصل على راتب الشهر كاملاً:
  - يعتمد النظام شهر عمل معياري مكوّن من **30 يوماً**.
  - إذا تعيّن موظف في يوم 10 من الشهر، يُحسب له 21 يوماً تلقائياً: `(الراتب ÷ 30) × 21`.

---

## 4. ملفات التهيئة والبنية التحتية

### 📄 `frontend/src/main.tsx`
- **المسار**: `d:\rab7a\frontend\src\main.tsx`
- **الوظيفة**: نقطة الدخول (Entry Point) للتطبيق.
- **ماذا يفعل الكود**:
  ```tsx
  import React from 'react';
  import ReactDOM from 'react-dom/client';
  import App from './App';
  import './index.css';

  ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
  ```
  يقوم بربط ملف الـ CSS العام، والبحث عن العنصر ذي المعرف `root` في ملف `index.html`، ويحقن تطبيق React داخله مع تفعيل وضع `StrictMode`.

---

### 📄 `frontend/src/App.tsx`
- **المسار**: `d:\rab7a\frontend\src\App.tsx`
- **الوظيفة**: المنسق العام ومزود التصميم (Design System Provider).
- **ماذا يفعل الكود**:
  - يستورد `ConfigProvider` ومحليات اللغة العربية `arEG` من مكتبة `antd`.
  - يحدد الاتجاه العام: `direction="rtl"`.
  - يحدد الـ Theme العام:
    - نوع الخط: `'Cairo, sans-serif'`.
    - اللون الرئيسي الموحد: `#1890ff`.
    - زوايا الحواف (Border Radius): `8px`.
  - يقوم باستدعاء الحاوية التنفيذية الكبرى `<EmployeePayrollModuleContainer />`.

---

### 📄 `frontend/src/index.css`
- **المسار**: `d:\rab7a\frontend\src\index.css`
- **الوظيفة**: ضبط الخطوط والقواعد المورثة والتنسيقات المشتركة.
- **أهم ما فيه**:
  - استيراد خط القاهرة الرسمي من Google Fonts: `@import url('https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700;800;900&display=swap');`.
  - تجهيز طبقات Tailwind: `@tailwind base; @tailwind components; @tailwind utilities;`.
  - ضبط شريط التمرير (Custom Scrollbar) ليظهر نحيفاً باللون الرمادي العصري ويتناسب مع التصميم بدلاً من الشريط الافتراضي القديم للمتصفح.
  - تعريف فئات الاتجاه (`.dir-rtl`).

---

### 📄 `frontend/vite.config.ts`
- **المسار**: `d:\rab7a\frontend\vite.config.ts`
- **الوظيفة**: إعداد بيئة الخادم والتجميع (Build Config).
- **أهم الإعدادات**:
  - `port: 3000`: تشغيل الواجهة دائماً على البورت 3000.
  - `resolve.alias: { '@': path.resolve(__dirname, './src') }`: إمكانية كتابة المسارات بشكل مختصر ونظيف مثل `@/components/common` بدلاً من المسارات النسبية الطويلة `../../`.
  - `server.proxy`: توجيه أي رابط يبدأ بـ `/api` تلقائياً إلى سيرفر Laravel على `http://127.0.0.1:8000`.

---

## 5. طبقة الاتصال بالخادم (API Layer)

### 📄 `frontend/src/api/client.ts`
- **المسار**: `d:\rab7a\frontend\src\api\client.ts`
- **الوظيفة**: إدارة جميع طلبات الشبكة (HTTP Requests).
- **شرح الكود ودوره**:
  1. `const API_BASE_URL = 'http://localhost:8000/api/v1';`: الرابط الأساسي لجميع واجهات البرمجة الخاصة بالإصدار الأول.
  2. `fetchApi<T>(endpoint, options)`: دالة موحدة (Generic Fetcher):
     - تضيف تلقائياً ترويسات `Content-Type: application/json` و `Accept: application/json`.
     - تتحقق من `response.ok`، وفي حال حدوث خطأ تستخرج رسالة الخطأ القادمة من الباك إند وترمي استثناءً واضحاً.
     - تستخرج `result.data` في حال كان الرد مغلفاً وفق معايير JSON:API.
  3. `hrApi`: كائن جاهز يضم دوال الاستدعاء المباشرة:
     - `getEmployees()`: جلب قائمة الموظفين `GET /hr/employees`.
     - `createEmployee(data)`: إنشاء موظف جديد `POST /hr/employees`.
     - `updateEmployee(id, data)`: تعديل موظف `PUT /hr/employees/{id}`.
     - `deleteEmployee(id)`: حذف/أرشفة موظف `DELETE /hr/employees/{id}`.
     - `getStatement(id)`: جلب كشف حساب الموظف `GET /hr/employees/{id}/statement`.

---

## 6. طبقة الأنواع والبيانات (Type System)

### 📄 `frontend/src/types/hr.types.ts`
- **المسار**: `d:\rab7a\frontend\src\types\hr.types.ts`
- **الواجهات والأنواع المعرفة**:
  - `Employee`:
    - `id`: المعرف الرقمي الفريد للموظف.
    - `name`: اسم الموظف ثلاثي أو رباعي.
    - `employee_code`: كود الموظف الوظيفي (مثل `EMP-001`).
    - `department`: القسم (مطبخ، صالة، إدارة، دليفري، تجهيزات).
    - `position`: المسمى الوظيفي (شيف، ويتر، محاسب، كاشير).
    - `base_salary`: الراتب الشهري التعاقدي (بالقرش).
    - `hire_date`: تاريخ التعيين بصيغة ISO (`YYYY-MM-DD`).
    - `status`: حالة الموظف (`active`, `on_leave`, `suspended`, `terminated`).
  - `EmployeeAccountLedgerEntry`:
    - قيد الحركة المحاسبي في دفتر أستاذ الموظف.
    - يتضمن نوع الحركة (`type`): سلفة (`advance`)، مكافأة (`bonus`)، خصم (`penalty`)، إضافي (`overtime`)، تأخير (`late`)، غياب (`absence`)، أو استحقاق راتب (`salary_accrual`).
    - القيمة بالقرش (`amount`)، والتاريخ، والبيان (`description`).
  - `EmployeeFinancialPosition`:
    - الموقف المالي الشامل للموظف لشهر محدد:
    - `totalAdvances`: إجمالي السلف المقبوضة خلال الشهر.
    - `totalBonuses`: إجمالي المكافآت.
    - `totalDeductions`: إجمالي الخصومات والغياب والتأخيرات.
    - `proratedBaseSalary`: الراتب الأساسي المحسوب بالتناسب الزمني.
    - `netPayable`: صافي الراتب المستحق للصرف في نهاية الشهر.

---

### 📄 `frontend/src/types/inventory.types.ts`
- **المسار**: `d:\rab7a\frontend\src\types\inventory.types.ts`
- **الواجهات المعرفة**:
  - `InventoryItem`: خامات وأصناف المخزن (الاسم، الوحدة مثل كجم ولتر، الرصيد المتاح، وحد إعادة الطلب، ومتوسط التكلفة WAC).
  - `StockMovement`: نوع الحركة المخزنية (`in` وارد، `out` منصرف للتشغيل، `waste` هالك، `adjustment` تسوية جردية).
  - `PurchaseOrder`: أمر الشراء الصادر للمورد وتفاصيل بنوده.
  - `Supplier`: سجل المورد وبيانات اتصاله والأرصدة المستحقة له.

---

## 7. الأدوات المساعدة والمكونات المشتركة

### 📄 `frontend/src/utils/money.ts`
- **المسار**: `d:\rab7a\frontend\src\utils\money.ts`
- **الدوال الرياضية**:
  - `formatMoney(piasters: number, showSymbol = true)`:
    - تأخذ القيمة بالقروش وتقسم على 100 لتحويلها إلى جنيهات.
    - تستخدم `Intl.NumberFormat('ar-EG', { minimumFractionDigits: 2 })` لضمان إخراج الرقم بالشكل المصري المنسق مع فاصلة الآلاف وعلامة الجنيه `ج.م`.
  - `toPiasters(egp: number)`:
    - تضرب أي رقم مدخل بالجنيه في 100 وتطبق `Math.round` لإنتاج عدد صحيح تام من القروش دون أي كسور.

---

### 📄 `frontend/src/components/common/MoneyDisplay.tsx`
- **المسار**: `d:\rab7a\frontend\src\components\common\MoneyDisplay.tsx`
- **الوظيفة**: مكون React موحد لعرض المبالغ النقدية على امتداد النظام.
- **المميزات**:
  - يقبل خاصية `type`:
    - `'positive'`: يلون الرقم بالأخضر الغامق `#2e7d32` (للمكافآت والاستحقاقات).
    - `'negative'`: يلون الرقم بالأحمر الغامق `#c62828` (للخصومات والسلف والاستقطاعات).
    - `'neutral'`: يترك اللون الافتراضي للنص.
  - يفرض اتجاه الأرقام من اليسار لليمين داخل الخلية (`direction: 'ltr'`) لضمان عدم انعكاس الأرقام والفاصلة العشرية في الواجهات العربية.

---

### 📄 `frontend/src/components/common/StatusBadge.tsx`
- **المسار**: `d:\rab7a\frontend\src\components\common\StatusBadge.tsx`
- **الوظيفة**: وسم أو شارة ملونة (Badge/Tag) لحالات الكائنات.
- **التصنيف اللوني**:
  - حالات النجاح والإيجابية (`active`, `paid`, `present`, `approved`): تأخذ لون **الأخضر (Success)**.
  - الحالات التمهيدية والمعلقة (`calculated`, `pending`, `on_leave`): تأخذ لون **البرتقالي الهادئ (Warning)**.
  - حالات الإنهاء والإلغاء والغياب (`terminated`, `cancelled`, `absent`, `archived`): تأخذ لون **الأحمر (Error)**.

---

## 8. محرك إدارة الحالة وقواعد العمل (HrContext)

### 📄 `frontend/src/context/HrContext.tsx`
- **المسار**: `d:\rab7a\frontend\src\context\HrContext.tsx`
- **الأهمية**: هو **عقل النظام** وأهم ملف في فرونت إند الموارد البشرية والرواتب. يجمع كافة قواعد العمل والمحاسبة ويوفرها لجميع الصفحات والمكونات عبر React Context (`useHr()`).

#### تفصيل الدوال وقواعد العمل في `HrContext`:

1. **`getProratedBaseSalary(emp, year, month)`**:
   - تأخذ بيانات الموظف والشهر المستهدف.
   - إذا كان تاريخ تعيين الموظف سابقاً للشهر، يعود الراتب كاملاً.
   - إذا تعين الموظف خلال الشهر (مثلاً يوم 12)، تحسب عدد الأيام التي عملها فعلياً: `30 - 12 + 1 = 19 يوماً`.
   - تحسب الراتب المتناسب: `(الراتب ÷ 30) × 19`.
   - تعيد كائناً يحتوي على: `{ proratedSalary, workedDays, totalDaysInMonth, isProrated }`.

2. **`getFinancialPosition(employeeId, year, month)`**:
   - تفلتر جميع قيود الدفتر المالي الخاصة بالموظف داخل هذا الشهر.
   - تجمع السلف (`advances`).
   - تجمع المكافآت والإضافي (`bonuses`, `overtime`).
   - تجمع الخصومات والغياب والتأخير (`deductions`).
   - تطبق معادلة الصافي:  
     $$\text{الصافي المستحق (Net Payable)} = \text{الراتب المتناسب} + \text{المكافآت} - \text{الخصومات} - \text{السلف}$$

3. **`addTransaction(params)`**:
   - تسجل حركة مالية جديدة وتضيفها لدفتر أستاذ الموظف فوراً.
   - تنشئ معرّفاً فريداً للحركة، وتحدد التاريخ، وتسجل الأثر المالي (دائن أو مدين).
   - تحفظ التغيير في التخزين المحلي وترسل إشارة المزامنة للباك إند.

4. **`payPayrollRun(year, month, paymentMethod)`**:
   - تقوم باعتماد وصرف مسير الرواتب لجميع موظفي المطعم للشهر دفعة واحدة.
   - تحول حالة المسير إلى `paid`.
   - تُنشئ قيود سداد رسمية في دفاتر الحسابات لمنع ازدواجية الصرف.

5. **`paySingleEmployeePayroll(employeeId, year, month, paymentMethod)`**:
   - تتيح صرف مرتب موظف محدد بمفرده مقدماً أو عند تصفيته.

---

## 9. هيكل التطبيق والتنقل العام

### 📄 `frontend/src/components/modules/EmployeePayrollModuleContainer.tsx`
- **المسار**: `d:\rab7a\frontend\src\components\modules\EmployeePayrollModuleContainer.tsx`
- **الوظيفة**: الشاشة الأم والحاوية الكبرى للنظام بالكامل (`AppShell`).
- **المكونات الداخلية**:
  1. **`HomePage` (لوحة البداية الرئيسية)**:
     - تعرض ترويسة أنيقة بشعار وهوية المطعم "مطعم رابحة — طعم البيوت".
     - كروت إحصائية تفاعلية: (عدد الموظفين، كتلة الأجور، أقسام المخزن والموردين).
     - عند الضغط على أي كارت يتم الانتقال فوراً للشاشة المعنية.
  2. **`AppShell` (إطار النظام)**:
     - **Header**: يحتوي على الشعار، عنوان النظام، وبيانات المستخدم (مدير النظام).
     - **Sider (القائمة الجانبية)**: قائمة مجهزة للأجهزة المكتبية والمحمولة تدعم الطي، مصنفة لأقسام:
       - *الصفحة الرئيسية*
       - *شؤون الموظفين* (قائمة الموظفين)
       - *الرواتب والحسابات* (لوحة المؤشرات، مسيرات الرواتب، كشف حساب موظف)
       - *سلاسل الإمداد والمخزون* (الأصناف والخامات، حركات المخزن، أوامر الشراء GRN، الموردين، كشف حساب مورد)
     - **مبدل الصفحات (`renderContent`)**: يحتوي على جملة `switch` تبدل الشاشات ديناميكياً حسب الـ `currentPage` الحالية بدون تحديث للمتصفح.

---

## 10. النوافذ المنبثقة التفاعلية (Modals)

### 📄 `frontend/src/components/modules/hr/EmployeeFormModal.tsx`
- **المسار**: `d:\rab7a\frontend\src\components\modules\hr\EmployeeFormModal.tsx`
- **الوظيفة**: نافذة نموذج إدخال وتعديل بيانات الموظف.
- **الحقول**:
  - الاسم الرباعي (مطلوب).
  - كود الموظف الفريد (مثل `EMP-005`).
  - القسم الوظيفي (مطبخ، صالة، دليفري، إدارة، كاشير).
  - الراتب الأساسي الشهري (يتم إدخاله بالجنيه ويقوم النموذج بتحويله تلقائياً لقروش).
  - تاريخ التعيين (محدد تاريخ هجري/ميلادي).
  - رقم الهاتف وحالة العمل (نشط، إجازة، موقوف).

---

### 📄 `frontend/src/components/modules/hr/QuickFinancialActionModal.tsx`
- **المسار**: `d:\rab7a\frontend\src\components\modules\hr\QuickFinancialActionModal.tsx`
- **الوظيفة**: نافذة الإجراءات المالية السريعة اليومية.
- **ماذا تفعل**:
  - تتيح لمشرف الفرع تسجيل أي حركة مالية طارئة خلال ثوانٍ.
  - تحتوي على قائمة منسدلة بالعمليات:
    - *سلفة نقدية*: خصم مباشر من صافي الراتب.
    - *مكافأة تشجيعية*: إضافة مستحقة للراتب.
    - *جزاء / خصم*: استقطاع مباشر.
    - *ساعات إضافية (Overtime)*: يقوم المستخدم بإدخال عدد الساعات ويحسب النظام قيمتها المالية آلياً.
    - *تأخير أو غياب*: إدخال عدد الساعات أو الأيام واحتساب أثرها الخصمي بدقة.

---

### 📄 `frontend/src/components/modules/hr/LedgerTransactionDetailModal.tsx`
- **المسار**: `d:\rab7a\frontend\src\components\modules\hr\LedgerTransactionDetailModal.tsx`
- **الوظيفة**: نافذة فحص ومراجعة القيد المالي الدفتري.
- **ماذا تعرض**: رقم القيد التسلسلي، الموظف المعني، نوع وتاريخ الحركة، المبلغ المالي بالجنيه، البيان والشرح، وتأثير الحركة الدفتري (دائن / مدين).

---

### 📄 `frontend/src/pages/payroll/PayrollDetailsModal.tsx`
- **المسار**: `d:\rab7a\frontend\src\pages\payroll/PayrollDetailsModal.tsx`
- **الوظيفة**: نافذة تفصيل ومراجعة مفردات راتب الموظف (Detailed Payslip Breakdown).
- **ماذا تفعل**:
  - تعرض كشفاً تفصيلياً يقسم البنود إلى جدولين:
    1. **الاستحقاقات (Earnings)**: الراتب الأساسي، التناسب الزمني إن وجد، المكافآت، وبدل الإضافي.
    2. **الاستقطاعات (Deductions)**: السلف المقبوضة، الجزاءات، خصم الغياب والتأخيرات.
  - تظهر مربع الصافي النهائي المستحق مع زر إجراء صرف فردي مباشر للموظف.

---

## 11. شاشات الموارد البشرية (HR Pages)

### 📄 `frontend/src/pages/hr/EmployeeListPage.tsx`
- **المسار**: `d:\rab7a\frontend\src\pages\hr\EmployeeListPage.tsx`
- **الوظيفة**: الشاشة الرئيسية لسجل وإدارة موظفي المطعم.
- **المكونات والخصائص**:
  - جدول Ant Design تفاعلي يعرض: الصورة، كود الموظف، الاسم، القسم، الوظيفة، الراتب الأساسي، الموقف المالي الحالي، والحالة.
  - **محرك البحث والفلترة**: بحث فوري بالاسم أو الكود، وقوائم فلترة فورية حسب القسم أو حالة العمل.
  - **الإجراءات السريعة (Actions Menu)**:
    - زر لإجراء مالي سريع (سلفة أو خصم).
    - زر فتح كشف الحساب المالي.
    - زر فتح الملف الشخصي الكامل.
    - خيارات تعديل البيانات أو الحذف.
  - إمكانية تصدير بيانات الموظفين والطباعة.

---

### 📄 `frontend/src/pages/hr/EmployeeProfilePage.tsx`
- **المسار**: `d:\rab7a\frontend\src\pages\hr\EmployeeProfilePage.tsx`
- **الوظيفة**: صفحة الملف الشخصي الشامل للموظف.
- **الأقسام**:
  - بطاقة علوية تحتوي على تفاصيل الوظيفة ومعلومات الاتصال والموقف المالي الحالي.
  - تبويب **نظرة عامة**: البيانات الشخصية، الرقم القومي، تاريخ التعيين، وسنوات الخدمة.
  - تبويب **الدفتر المالي (Financial Ledger)**: سجل تاريخي بجميع القيود التي تمت على حسابه منذ تعيينه.
  - تبويب **إدارة الحساب**: إمكانية تعديل البيانات، تجميد الحساب، أو إنهاء الخدمة.

---

### 📄 `frontend/src/pages/hr/EmployeeStatementPage.tsx`
- **المسار**: `d:\rab7a\frontend\src\pages\hr\EmployeeStatementPage.tsx`
- **الوظيفة**: كشف الحساب المالي الرسمي للموظف (Statement of Account).
- **المميزات**:
  - مبني بمفهوم دفتري دقيق: (تاريخ الحركة، البيان، مدين، دائن، والرصيد التراكمي بعد الحركة).
  - مجهز للطباعة الورقية الرسمية (Print Ready) لتسليمه للموظف عند التصفية أو نهاية الشهر مع مكان لتوقيع الموظف وتوقيع الإدارة.

---

## 12. شاشات الرواتب ومسيرات الصرف

### 📄 `frontend/src/pages/payroll/PayrollDashboardWidget.tsx`
- **المسار**: `d:\rab7a\frontend\src\pages\payroll\PayrollDashboardWidget.tsx`
- **الوظيفة**: لوحة المؤشرات المالية الشهرية للرواتب.
- **المؤشرات المعروضة**:
  - إجمالي المرتبات الأساسية لكافة العاملين.
  - إجمالي السلف المستقطعة لشهر الصرف.
  - صافي السيولة النقدية المطلوبة لتغطية رواتب الشهر.
  - مؤشرات توزيع تكلفة العمالة حسب أقسام المطعم (المطبخ، الصالة، التوصيل، الإدارة).

---

### 📄 `frontend/src/pages/payroll/PayrollListPage.tsx`
- **المسار**: `d:\rab7a\frontend\src\pages\payroll\PayrollListPage.tsx`
- **الوظيفة**: شاشة مسيرات الرواتب الشهرية والاعتماد المالي.
- **دورة العمل في الشاشة**:
  1. اختيار الشهر والسنة (مثلاً يوليو 2026).
  2. الضغط على زر **احتساب الرواتب**: يقوم النظام بقراءة الرواتب والسلف والخصومات وحساب الصافي لكل موظف.
  3. استعراض جدول المسير وتدقيق أي موظف عبر نافذة التفاصيل.
  4. الضغط على زر **اعتماد المسير**، ثم **صرف الرواتب** بالكامل مع اختيار طريقة الدفع (خزينة المطعم نقداً أو تحويل بنكي).

---

## 13. شاشات سلاسل الإمداد والمخزون

### 📄 `frontend/src/pages/inventory/InventoryListPage.tsx`
- **المسار**: `d:\rab7a\frontend\src\pages\inventory\InventoryListPage.tsx`
- **الوظيفة**: جرد خامات المطبخ والمواد الغذائية.
- **المميزات**:
  - قائمة بالخامات (لحوم، خضروات، زيوت، بهارات، مواد تغليف).
  - عرض الرصيد المتاح حالياً مع تنبيهات عند اقتراب نفاذ الصنف (الوصول لحد إعادة الطلب).
  - احتساب متوسط التكلفة المرجح (WAC) لمتابعة تكلفة الوجبات والأطباق بدقة.
  - تسجيل حركات الهالك (Waste) وتحديد الأسباب وقيمتها المالية.

---

### 📄 `frontend/src/pages/inventory/StockMovementsPage.tsx`
- **المسار**: `d:\rab7a\frontend\src\pages\inventory\StockMovementsPage.tsx`
- **الوظيفة**: سجل تدقيق حركة المخازن.
- **المميزات**: تتبع زمني لكل كيلو أو لتر يدخل أو يخرج من مخازن المطعم (وارد مشتريات، منصرف للمطبخ، هالك، أو تعديل جردي) مع بيان المسؤول وتاريخ العملية.

---

### 📄 `frontend/src/pages/purchases/PurchaseOrderListPage.tsx`
- **المسار**: `d:\rab7a\frontend\src\pages\purchases\PurchaseOrderListPage.tsx`
- **الوظيفة**: إدارة أوامر الشراء وإذن الاستلام الفعلي بالمخزن (GRN).
- **المميزات**:
  - تسجيل طلبات الشراء الصادرة لموردي المواد الغذائية.
  - ميزة **إذن الاستلام الفعلي (Goods Receipt Note - GRN)**: عند وصول الشاحنة إلى المطعم، يراجع أمين المخزن البضاعة ويؤكد استلامها، فيتم تلقائياً:
    1. زيادة رصيد الأصناف في المخزن.
    2. إعادة احتساب متوسط سعر التكلفة المرجح (WAC).
    3. تسجيل مديونية الفاتورة في كشف حساب المورد.

---

### 📄 `frontend/src/pages/suppliers/SupplierListPage.tsx` & `SupplierStatementPage.tsx`
- **المسارات**:
  - `d:\rab7a\frontend\src\pages\suppliers\SupplierListPage.tsx`
  - `d:\rab7a\frontend\src\pages\suppliers\SupplierStatementPage.tsx`
- **الوظيفة**: إدارة علاقات وحسابات الموردين.
- **المميزات**:
  - دليل الموردين، الهاتف، شروط التوريد، وإجمالي المبالغ المستحقة لهم.
  - كشف حساب المورد التفصيلي: يعرض كافة فواتير التوريد المقيدة له، والدفعات المسددة له من الخزينة، وصافي الرصيد المتبقي له.

---

## 14. سيناريوهات دورة العمل خطوة بخطوة

### 🔄 السيناريو الأول: موظف جديد وصرف سلفة
1. يفتح المشرف قائمة الموظفين (`EmployeeListPage`) ويضغط **إضافة موظف**.
2. يُدخل الاسم، القسم (مثلاً: المطبخ)، الوظيفة (طباخ)، والراتب الأساسي (مثلاً: 6000 جنيه).
3. يتم تحويل الـ 6000 جنيه تلقائياً إلى `600000` قرش في `HrContext` وتخزين الموظف.
4. بعد أسبوع طلب الموظف سلفة 500 جنيه: يضغط المشرف على **إجراء سريع** بجانب اسمه ويختار **سلفة** ويكتب 500.
5. يسجل النظام حركة دائنة في دفتر حسابه ويخصم 500 جنيه من صافي استحقاقه لشهر الصرف فوراً.

### 🔄 السيناريو الثاني: توليد وصرف مسير الرواتب الشهري
1. في نهاية الشهر، ينتقل المشرف لصفحة **مسيرات الرواتب** (`PayrollListPage`).
2. يختار شهر الاستحقاق ويضغط **احتساب المسير**.
3. يقوم النظام آلياً بفحص كل موظف:
   - يحسب الراتب التناسبي بناءً على تاريخ تعيينه.
   - يستقطع السلف المسجلة خلال الشهر.
   - يضيف المكافآت ويخصم الجزاءات والغياب.
   - يخرج صافي المبلغ المستحق بدقة.
4. يراجع المشرف المسير، يضغط **اعتماد المسير**، ثم يضغط **صرف الرواتب** نقداً من خزينة المطعم، فيتحول المسير إلى `paid` وتُغلق حسابات الشهر.

---

## 15. دليل الإجابة على الأسئلة التقنية (FAQ)

عندما يسألك أي شخص أو يناقشك في تفاصيل الفرونت إند، يمكنك الاستعانة بهذه الإجابات الاحترافية:

**س1: لماذا استخدمتم Context API بدلاً من Redux في إدارة الحالة؟**  
> *"لأن نطاق التطبيق الحالي وعدد الوحدات تم تنظيمه عبر معمارية مقسمة بعناية، فـ Context API المدمج في React يوفر أداءً خفيفاً وفائق السرعة دون التعقيد البرمجي الإضافي والـ Boilerplate الكبير لـ Redux، مع ربطه بآلية حفظ مزدوجة (LocalStorage + Backend Sync) تضمن أمان وسرعة البيانات."*

**س2: كيف تضمنون عدم حدوث أخطاء حسابية في الرواتب والمبالغ المالية في الجافاسكريبت؟**  
> *"نطبق قاعدة القرش الصارم (The Penny Rule)، حيث تُخزن كافة المبالغ المالية كأعداد صحيحة بالقروش (Integers)، مما يلغي كلياً أخطاء الفواصل العشرية (Floating Point Precision) المعروفة في الجافاسكريبت. وتتم القسمة على 100 فقط في طبقة العرض عبر دالة `formatMoney` الموحدة."*

**س3: كيف تم ضبط النظام ليعمل باللغة العربية واتجاه RTL بهذه الدقة؟**  
> *"تم دمج `ConfigProvider` من مكتبة Ant Design على مستوى الجذر وضبط `direction="rtl"` ومحليات `ar_EG`، مع استيراد خط 'Cairo' الرسمي وتنسيقات الـ CSS المخصصة التي تحافظ على محاذاة الأرقام المالية بالـ LTR داخل النصوص العربية لمنع تداخل العلامات."*

**س4: ما هي الميزة المحاسبية في شاشة المشتريات (GRN)؟**  
> *"النظام يطبق دورة الشراء الفعلية (Goods Receipt Note)؛ فأمر الشراء لا يغير المخزن إلا عند تأكيد أمين المخزن للاستلام الفعلي، وحينها فقط يتم تحديث رصيد الصنف وتحديث متوسط التكلفة المرجح (WAC) وتقييد الفاتورة في حساب المورد."*

---

*هذا الملف تم إنشاؤه وتوثيقه بالكامل ليكون دليلك الشامل لفرونت إند **Rabha ERP**.*
